# Frontend (placeholder)

This project currently doesn't include a full frontend. This folder is a placeholder for a separate frontend application (React, Angular, Vue, etc.).

Quick start (placeholder):

- Create your frontend app here (for example `npx create-react-app .`).
- Configure CORS in the backend if the frontend runs on a different origin.

